# User-Management-Syestem
The task is to build a full-stack User Management System using React.js (frontend) and Node.js/Express.js with MySQL (backend) that includes JWT authentication, secure user registration, an Admin Dashboard for CRUD operations, and proper input validation
